## 1. CSS Extraction

- [ ] 1.1 Create the `style.css` file and copy all internal styles from `index.html`.
- [ ] 1.2 Replace the `<style>` tag in `index.html` with a `<link rel="stylesheet" href="style.css">` tag.

## 2. JavaScript Extraction

- [ ] 2.1 Create the `script.js` file and copy all internal scripts from `index.html`.
- [ ] 2.2 Replace the `<script>` tag in `index.html` with a `<script src="script.js"></script>` tag.

## 3. Verification

- [ ] 3.1 Verify visual appearance remains consistent.
- [ ] 3.2 Verify language switching functionality.
- [ ] 3.3 Verify scroll animations and the site guide's behavior.
